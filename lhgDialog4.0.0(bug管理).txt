BUG 测试管理

2011-11-26 
	1. 弹出带lock的窗口，窗口里(弹出url页面)点击按钮，调用hide()方法隐藏当前窗口，报了一个错（this[0] is undefined），窗口隐藏了，但是遮罩层没有隐藏。

	2. 弹出窗口没有隐藏右上角关闭按钮的设置，例如做登录的时候，弹出遮罩层，同时窗口不能关闭，我通过close回调函数改变右上角关闭按钮的方法，但是这样后，再也无法调用close来关闭窗口。

	3. IE6 的锁屏也不是全屏的

	4. 如果内部是 iframe拖动到右侧贴边会造成窗口变形



